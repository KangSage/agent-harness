# /project-prompt

Canonical alias for `/prompt`.

This alias must remain behaviorally equivalent to `/prompt`.

Do not define separate mode behavior here. Keep `/prompt` as the source command contract and use this file only as the stable alias surface.
