name: Validate Scaffold

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run scaffold validation
        run: bash /home/runner/work/agent-harness/agent-harness/packages/project-prompt-kit/scripts/validate.sh
